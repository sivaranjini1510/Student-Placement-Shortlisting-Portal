export const ROLES = {
  ADMIN: 'admin',
  STUDENT: 'student',
  STAFF: 'staff',
};

export const EMAIL_REGEX = /^[a-zA-Z0-9._%+-]+@nec\.edu\.in$/;
export const PHONE_REGEX = /^\d{10}$/;
export const URL_REGEX = /^(https?:\/\/)?([\w\d-]+\.)+\w{2,}(\/.*)?$/;
